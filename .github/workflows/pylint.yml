name: Auto Linter

on: 
  push:
    paths: "*.py"

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
       - name: Checkout
         uses: actions/checkout@v7

       - name: Install python linter
         run: pip install ruff
      
      
